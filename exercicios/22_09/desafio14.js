var pilotoAlturaMin = 0;
var pilotoAlturaMax = 0;

var jogadorAlturaMin = 0;
var jogadorAlturaMin = 0;

function selecionar(){
    pilotoAlturaMin = Number(ipt_pilotoAlturaMin);
    pilotoAlturaMax = Number(ipt_pilotoAlturaMax);

    jogadorAlturaMin = Number(ipt_jogadorAlturaMin);
    jogadorAlturaMin = Number(ipt_jogadorAlturaMax);

    if()
}